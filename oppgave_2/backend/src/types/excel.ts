export type Event = {
  title: string;
  location: string;
};

export type RegistrationWithEventByYear = {
  event_title: string;
  event_location: string;
  email: string;
  has_paid: string;
  registration_date: string;
};
